
const dbConfig = require('../database/dbclient');

const nodeCron = require("node-cron");

export const userDatabaseUpdater = async () => {
    try {
        // const databaseUpdaterjob = nodeCron.schedule("*/30 * * * *", async () => {

        //     console.log('eded');

        // });


    } catch (e) {

    }

}

